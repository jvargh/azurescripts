# Azure Health Model Configuration
# These environment variables are used by health model scripts
# 
# SETUP INSTRUCTIONS:
# 1. Copy this file and rename it to .env
# 2. Fill in your Azure values below
# 3. Run: python run.py

# Azure Subscription ID (REQUIRED)
# Get from: az account show --query id -o tsv
AZURE_SUBSCRIPTION_ID=

# Azure Resource Group (REQUIRED)
# The resource group containing your health model
AZURE_RESOURCE_GROUP=

# Health Model Name (REQUIRED)
# The name of your Azure Health Model
HEALTH_MODEL_NAME=

# Azure Tenant ID (OPTIONAL - will use default if not set)
# Get from: az account show --query tenantId -o tsv
AZURE_TENANT_ID=

# Auth Token (OPTIONAL - will be fetched from Azure CLI if not set)
# If not provided, token will be obtained automatically via 'az account get-access-token'
AZURE_AUTH_TOKEN=
